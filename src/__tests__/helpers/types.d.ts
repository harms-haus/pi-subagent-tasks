// Ambient declarations reserved for the test harness.
// Currently empty; project-wide ambient needs (e.g. module augmentations) can
// be added here. Excluded from coverage via the vitest "src/**/*.d.ts" rule.
export {};
